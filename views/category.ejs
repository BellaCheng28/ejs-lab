<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include('./partials/nav') %>
    <h1><%= menuItems.name %></h1>
    <ul><% menuItems.forEach(element => { %> 
      <li>
        <p>name</p><%= element.name %>
        <p>price</p><%= element.price %>
        <p>rating</p><%= element.rating %>
        <p>details</p><%= element.details %>
      </li>
      <%})%>
   </ul>

    
  </body>
</html>
